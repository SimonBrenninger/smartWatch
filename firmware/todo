
* organize individual modules into ./lib subfolders (./lib/mxc400x) and add (if neccessary) multiple HAL layers
* setup zephyr's test tools
* create hardware-independent wrapper for the target hardware
* use unittesting
* create feature branch to add ble functionality
* use individual branches for each ble functionality (volume & music control, heartrate sensor, etc)
